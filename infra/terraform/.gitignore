# Terraform state may contain secrets (static access keys) — never commit it.
*.tfstate
*.tfstate.*
.terraform/
# .terraform.lock.hcl is committed on purpose to pin provider versions.

# Local variable overrides / secrets
terraform.tfvars
*.auto.tfvars
crash.log
crash.*.log

# Plans can embed secrets
*.tfplan
